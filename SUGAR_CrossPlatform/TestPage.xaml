<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="SUGAR_CrossPlatform.TestPage">
    <ContentPage.Content>
        <ScrollView>
        <StackLayout
            Padding="40">
            <Button
                x:Name="FetchContactsButton"
                Text="Fetch Contacts"
                Clicked="FetchContacts"/>
            <!--<Button
                x:Name="RequestAccessButton"
                Text="Request Access"
                Clicked="RequestAccess"/>-->
            <!--<Button
                x:Name="ShowNumberButton"
                Text="Show Number"
                Clicked="ShowNumber"/>-->
            <!--<Button
                x:Name="ShowNormalizedButton"
                Text="Show Normalized"
                Clicked="ShowNormalized"/>-->
            <!--<Button
                x:Name="DisplayCountryCodeButton"
                Text="Display Country Code"
                Clicked="DisplayCountryCode"/>-->
            <!--<Button
                x:Name="ShowLongButton"
                Text="Show Number as Long"
                Clicked="ShowLong"/>-->
            <Label
                x:Name="ResultLabel"/>
        </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>
